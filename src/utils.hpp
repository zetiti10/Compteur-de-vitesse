#ifndef UTILS_DEFINITIONS
#define UTILS_DEFINITIONS

#include <Arduino.h>

String addZeros(int number, int length);
float mapFloat(float x, float  inMin, float inMax, float outMin, float outMax);

#endif